apiVersion: v1
kind: ConfigMap
metadata:
  name: cloud-82202374-app-config
  namespace: default
data:
  application.yml: |
    spring:
      profiles: prod
      datasource:
        url: jdbc:postgresql://localhost:5432/your_database
        username: ktds
        password: ${DB_PASSWORD} 